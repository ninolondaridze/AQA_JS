stages:
  - test

image: ghcr.io/puppeteer/puppeteer:latest

before_script:
  - npm ci

UI test_1:
  stage: test
  script:
    - cd UI_tests_1
    - chromedriver --version
    - google-chrome --version
    - npm run test